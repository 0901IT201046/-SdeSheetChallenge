#include<bits/stdc++.h>
vector<int> kthSmallLarge(vector<int> &arr, int n, int k)
{
	sort(arr.begin(),arr.end());
    int i;
    int s1 = arr[k-1];
    int s2 = arr[n-k];
    vector<int> v;
    v.push_back(s1);
    v.push_back(s2);
    return v;
}
